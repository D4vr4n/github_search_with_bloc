name: github_search_with_bloc
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  # state management
  flutter_bloc: ^7.3.3
  #freezed
  freezed_annotation: ^0.14.1
  http: ^0.13.4
  stream_transform: ^2.0.0
  url_launcher: ^6.0.0

dev_dependencies:
  build_runner:
  freezed: ^0.14.1+1
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0


flutter:

  
  uses-material-design: true

  